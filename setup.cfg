[flake8]
# Only the pyflakes checks: real defects (unused imports, undefined names,
# shadowed definitions) rather than formatting opinions. Style is not enforced
# here — a linter arguing about line breaks adds noise without catching bugs.
select = F
exclude = .venv,.git,__pycache__,uploads_tmp,drafts_tmp
