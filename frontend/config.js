/* Deployment-time public configuration. This file must never contain secrets. */
window.SURVEYMIND_CONFIG = Object.freeze({
  // Leave blank for local development (defaults to http://127.0.0.1:8000).
  // Set to the public Render Web Service URL for the hosted frontend.
  apiBaseUrl: "",
});
